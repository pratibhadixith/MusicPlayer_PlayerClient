<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.prati.PlayerClient.MainActivity">

    <Button
        android:text="View Transactions !"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/transactions"
        android:layout_weight="1"
        android:layout_marginBottom="100dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true" />

    <LinearLayout
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_marginTop="67dp"
        android:layout_height="100dp"
        android:layout_below="@+id/audionumber"
        android:layout_alignParentStart="true">

        <ImageButton
            android:layout_height="78dp"
            app:srcCompat="@drawable/pause"
            android:id="@+id/pause"
            android:layout_width="76dp"
            android:scaleType="fitXY"
            android:padding="0dp"
            android:layout_marginLeft="50dp" />

        <ImageButton
            android:layout_width="82dp"
            android:layout_height="81dp"
            app:srcCompat="@drawable/play"
            android:id="@+id/play"
            android:scaleType="fitXY"
            android:layout_gravity="center_horizontal"
            android:padding="0dp" />

        <ImageButton
            android:layout_width="82dp"
            android:layout_height="80dp"
            app:srcCompat="@drawable/stop"
            android:id="@+id/stop"
            android:scaleType="fitXY"
            android:padding="0dp" />

    </LinearLayout>

    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:inputType="textPersonName"
        android:ems="10"
        android:id="@+id/audionumber"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true"
        android:layout_marginTop="38dp" />

    <TextView
        android:text="Enter the clip number below ! (1-5)"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/textView"
        android:textSize="18sp"
        tools:text="Enter the clip number below ! (1-5)" />

</RelativeLayout>
